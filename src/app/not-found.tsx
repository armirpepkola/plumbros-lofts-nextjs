import NotFoundContent from '@/components/NotFoundContent';

export default function NotFound() {
    // This is now a clean Server Component that renders our interactive Client Component.
    return <NotFoundContent />;
}